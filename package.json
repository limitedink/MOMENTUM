{
  "name": "momentum",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:playtest": "node scripts/tauri-playtest.mjs",
    "backend:dev": "npm --prefix backend run dev",
    "backend:lan": "HOST=0.0.0.0 PORT=3000 CORS_ORIGIN=http://tauri.localhost,https://tauri.localhost,tauri://localhost npm run backend:dev",
    "backend:build": "npm --prefix backend run build",
    "backend:start": "npm --prefix backend run start",
    "backend:typecheck": "npm --prefix backend run typecheck",
    "backend:test": "npm --prefix backend run test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^22.15.3",
    "jsdom": "^29.1.1",
    "sass": "^1.86.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
